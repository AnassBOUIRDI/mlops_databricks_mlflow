bundle:
  name: mlops-train-job

resources:
  jobs:
    train-job:
      name: ML MLOps Smallest Non-Photon Job (Bundle)
      tasks:
        - task_key: train_model
          description: "Train diabetes regression model"
          new_cluster:
            spark_version: 13.3.x-scala2.12
            node_type_id: Standard_DS3_v2  # 4 vCPU, 14 Go RAM (Azure)
            num_workers: 1
            data_security_mode: SINGLE_USER
          spark_python_task:
            python_file: /Workspace/Users/anass.b.compt@gmail.com/.bundle/mlops-train-job/default/files/train.py 